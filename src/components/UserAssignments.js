import Row from "react-bootstrap/Row"


export const UserAssignemnts = (props) => {
    const user_assignments = props.user.assignments
    const all_assignments = props.assignments


    const 
    return 
}